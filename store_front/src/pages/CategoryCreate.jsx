import { useState } from "react";import { useNavigate } from "react-router-dom";import { api } from "../api/endpoints";import CategoryForm from "./CategoryForm";export default function CategoryCreate(){const [form,setForm]=useState({name:'',description:'',is_active:true}); const nav=useNavigate(); const submit=async(e)=>{e.preventDefault();await api.createCategory(form);nav('/admin/categorias');}; return <CategoryForm form={form} onChange={(k,v)=>setForm({...form,[k]:v})} onSubmit={submit}/>;}