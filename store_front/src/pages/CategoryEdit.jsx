import { useEffect, useState } from "react";import { useNavigate, useParams } from "react-router-dom";import { api } from "../api/endpoints";import CategoryForm from "./CategoryForm";export default function CategoryEdit(){const {id}=useParams();const [form,setForm]=useState({name:'',description:'',is_active:true});const nav=useNavigate();useEffect(()=>{api.getCategory(id).then(({data})=>setForm(data));},[id]); const submit=async(e)=>{e.preventDefault();await api.updateCategory(id,form);nav('/admin/categorias');}; return <CategoryForm form={form} onChange={(k,v)=>setForm({...form,[k]:v})} onSubmit={submit}/>;}